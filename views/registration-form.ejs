<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>

    <style>
        /* --- 1. THEME VARIABLES (Light & Dark) --- */
        :root {
            /* Light Mode Defaults */
            --bg-body: #f4f7f6;
            --bg-card: #ffffff;
            --text-main: #2c3e50;       /* Dark Blue-Grey */
            --text-muted: #555555;      /* Medium Grey */
            --border-color: #dcdcdc;
            
            --input-bg: #ffffff;
            --input-border: #cccccc;
            --input-text: #333333;
            
            --btn-bg: #27ae60;          /* Green */
            --btn-hover: #219150;
            --btn-text: #ffffff;
        }

        /* Dark Mode Overrides */
        @media (prefers-color-scheme: dark) {
            :root {
                --bg-body: #121212;
                --bg-card: #1e1e1e;
                --text-main: #ecf0f1;       /* Off-White */
                --text-muted: #b2bec3;      /* Light Grey */
                --border-color: #333333;
                
                --input-bg: #2d3436;
                --input-border: #444444;
                --input-text: #ffffff;
                
                --btn-bg: #27ae60;          /* Keep Green */
                --btn-hover: #2ecc71;       /* Brighter Green on Hover */
            }
        }

        /* --- 2. BASE RESET & LAYOUT --- */
        body {
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-body);
            color: var(--text-main);
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: flex-start; /* Starts from top, allows scrolling */
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 500px; /* Limits width on Desktop */
            margin-top: 20px;
        }

        /* --- 3. CARD DESIGN --- */
        .card {
            background-color: var(--bg-card);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: 1px solid var(--border-color);
        }

        h3 {
            margin-top: 0;
            color: var(--text-main);
            text-align: center;
            font-size: 24px;
            margin-bottom: 5px;
        }

        p.subtitle {
            text-align: center;
            color: var(--text-muted);
            font-size: 14px;
            margin-bottom: 25px;
            margin-top: 0;
        }

        /* --- 4. FORM ELEMENTS --- */
        label {
            display: block;
            font-weight: 600;
            color: var(--text-muted);
            margin-bottom: 8px;
            font-size: 14px;
        }

        input, select {
            width: 100%;
            padding: 12px;
            margin-bottom: 18px;
            
            background-color: var(--input-bg);
            color: var(--input-text);
            border: 1px solid var(--input-border);
            border-radius: 6px;
            
            font-size: 16px; /* Prevents zoom on iPhone */
            box-sizing: border-box;
            
            /* Ensures Date Picker is Dark in Dark Mode */
            color-scheme: light dark; 
        }

        input:focus, select:focus {
            outline: none;
            border-color: var(--btn-bg);
            box-shadow: 0 0 0 3px rgba(39, 174, 96, 0.2); /* Green Glow */
        }

        /* Flex Row for Side-by-Side Inputs */
        .form-row {
            display: flex;
            gap: 15px;
        }
        .form-group {
            flex: 1; /* Each takes 50% width */
        }

        /* --- 5. BUTTON --- */
        .btn-submit {
            width: 100%;
            padding: 14px;
            background-color: var(--btn-bg);
            color: var(--btn-text);
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
            margin-top: 10px;
        }

        .btn-submit:hover {
            background-color: var(--btn-hover);
        }

        /* --- 6. BACK LINK --- */
        .back-link {
            text-align: center;
            margin-top: 20px;
        }
        .back-link a {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 14px;
        }
        .back-link a:hover {
            color: var(--btn-bg);
            text-decoration: underline;
        }

    </style>
</head>
<body>

    <div class="container">
        
        <div class="card">
            <h3>One Time Registration</h3>
            <p class="subtitle">Please fill in your details correctly.</p>

            <form action="/student/register" method="POST">
                
                <label>Mobile Number</label>
                <input type="text" name="mobile" placeholder="10-digit Mobile Number" required pattern="[0-9]{10}" title="Enter a valid 10-digit mobile number">

                <label>Create Password</label>
                <input type="password" name="password" placeholder="Set a strong password" required minlength="6">

                <label>Full Name</label>
                <input type="text" name="name" placeholder="Student Name" required>
                
                <label>Father's Name</label>
                <input type="text" name="fatherName" placeholder="Father's Name" required>
                
                <label>Date of Birth</label>
                <input type="date" name="dob" required>

                <div class="form-row">
                    <div class="form-group">
                        <label>Class</label>
                        <select name="studentClass" required>
                            <option value="" disabled selected>Select</option>
                            <% for(let i=6; i<=12; i++){ %>
                                <option value="<%= i %>">Class <%= i %></option>
                            <% } %>
                        </select>
                    </div>

                    <div class="form-group">
                        <label>Medium</label>
                        <select name="medium" required>
                            <option value="Hindi">Hindi</option>
                            <option value="English">English</option>
                        </select>
                    </div>
                </div>

                <label>Gender</label>
                <select name="gender" required>
                    <option value="" disabled selected>Select Gender</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                </select>

                <label>School Name </label>
                <input type="text" name="schoolName" placeholder="Enter School Name"required>
                
                <button type="submit" class="btn-submit">
                    Create Account
                </button>

            </form>

            <div class="back-link">
                <a href="/login">Already have an account? Login</a>
            </div>
        </div>
    </div>

</body>
</html>